resource "dns_a_record_set" "www" {
  zone = "dns1.lab."
  name = "www"
  addresses = [
    "127.0.0.1",
    "127.0.0.53"
  ]
}

resource "dns_a_record_set" "subdomain_1_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-1"
  addresses = [
    "100.64.1.1",
  ]
}

resource "dns_a_record_set" "subdomain_2_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-2"
  addresses = [
    "100.64.1.2",
  ]
}

resource "dns_a_record_set" "subdomain_3_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-3"
  addresses = [
    "100.64.1.3",
  ]
}

resource "dns_a_record_set" "subdomain_4_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-4"
  addresses = [
    "100.64.1.4",
  ]
}

resource "dns_a_record_set" "subdomain_5_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-5"
  addresses = [
    "100.64.1.5",
  ]
}

resource "dns_a_record_set" "subdomain_6_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-6"
  addresses = [
    "100.64.1.6",
  ]
}

resource "dns_a_record_set" "subdomain_7_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-7"
  addresses = [
    "100.64.1.7",
  ]
}

resource "dns_a_record_set" "subdomain_8_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-8"
  addresses = [
    "100.64.1.8",
  ]
}

resource "dns_a_record_set" "subdomain_9_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-9"
  addresses = [
    "100.64.1.9",
  ]
}

resource "dns_a_record_set" "subdomain_10_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-10"
  addresses = [
    "100.64.1.10",
  ]
}

resource "dns_a_record_set" "subdomain_11_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-11"
  addresses = [
    "100.64.1.11",
  ]
}

resource "dns_a_record_set" "subdomain_12_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-12"
  addresses = [
    "100.64.1.12",
  ]
}

resource "dns_a_record_set" "subdomain_13_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-13"
  addresses = [
    "100.64.1.13",
  ]
}

resource "dns_a_record_set" "subdomain_14_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-14"
  addresses = [
    "100.64.1.14",
  ]
}

resource "dns_a_record_set" "subdomain_15_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-15"
  addresses = [
    "100.64.1.15",
  ]
}

resource "dns_a_record_set" "subdomain_16_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-16"
  addresses = [
    "100.64.1.16",
  ]
}

resource "dns_a_record_set" "subdomain_17_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-17"
  addresses = [
    "100.64.1.17",
  ]
}

resource "dns_a_record_set" "subdomain_18_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-18"
  addresses = [
    "100.64.1.18",
  ]
}

resource "dns_a_record_set" "subdomain_19_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-19"
  addresses = [
    "100.64.1.19",
  ]
}

resource "dns_a_record_set" "subdomain_20_dns1_lab" {
  zone = "dns1.lab."
  name = "subdomain-20"
  addresses = [
    "100.64.1.20",
  ]
}
